/** Per-app identity. The only file that differs between our hackathon repos. */
export const APP_SLUG = "quote-arena";
/** Prefix for per-case email routing codes, e.g. QA-7F3K. */
export const CASE_PREFIX = "QA";
export const APP_NAME = "Quote Arena";
